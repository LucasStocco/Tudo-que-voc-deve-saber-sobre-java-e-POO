O que é visibilidade em um objeto? 
>> Linguagem de Modelagem Unificada:

A Linguagem de Modelagem Unificada (UML - Unified Modeling Language) é uma ferramenta para visualizar, documentar e planejar o desenvolvimento de sistemas orientados a objetos, incluindo aqueles feitos em Java.

Ela não é uma linguagem de programação, mas sim uma linguagem de modelagem que ajuda os desenvolvedores a entenderem e projetarem um sistema antes de escreverem código.

>> Como a UML se relaciona com Java?

-- Ajuda a planejar o código: Antes de programar, você pode criar diagramas UML para organizar as classes, métodos e interações do sistema.

-- Facilita a comunicação: Se várias pessoas estão trabalhando no projeto, os diagramas UML ajudam a todos entenderem como o sistema funciona.

-- Documenta o sistema: Serve como referência para futuras manutenções no código.
Principais Diagramas UML usados em Java:

1.Diagrama de Classes → Mostra as classes, atributos, métodos e relacionamentos.

2.Diagrama de Objetos → Representa instâncias específicas das classes em determinado momento.

3.Diagrama de Sequência → Explica como os objetos interagem ao longo do tempo.

4.Diagrama de Casos de Uso → Mostra como os usuários interagem com o sistema.

Em resumo, a UML ajuda a estruturar o código Java antes de programar, tornando o desenvolvimento mais organizado e compreensível.

--------------------------------------------------------------------------------------
>> Modificadores de visibilidade/acesso:

Os modificadores de visibilidade (ou de acesso) em Java controlam quem pode acessar atributos e métodos de uma classe. Eles são fundamentais para proteger os dados e garantir um bom encapsulamento.

Principais Modificadores de Visibilidade:
public (público)

1.0. Pode ser acessado de qualquer lugar dentro e fora da classe.
Exemplo:

public class Pessoa {
    public String nome;
}
private (privado)

1.2. Pode ser acessado somente dentro da própria classe.
Exemplo:

public class Pessoa {
    private int idade;
}

1.3. Para acessar um atributo private, usamos métodos getters e setters:

public int getIdade() {
    return idade;
}

public void setIdade(int idade) {
    this.idade = idade;
}
protected (protegido)

1.4.Pode ser acessado na própria classe, nas subclasses e no mesmo pacote.
Exemplo:

public class Animal {
    protected String especie;
}

1.5. Se outra classe for uma subclasse, ela pode acessar o atributo especie diretamente.
Sem modificador (default ou "package-private")

Se nenhum modificador for especificado, o acesso fica restrito ao mesmo pacote.
Exemplo:

class Carro {
    String modelo; // Só pode ser acessado dentro do mesmo pacote
}

Resumo em Tabela:

Modificador	Mesma Classe	Mesmo Pacote	Subclasses	Fora da Classe
(+)public	✅		✅		✅		✅
(#)protected	✅		✅		✅		❌
(default)	✅		✅		❌		❌
(-)private	✅		❌		❌		❌

💡 Dica:

→ Use private para proteger dados e só permitir acesso com métodos específicos.
→ Use public apenas quando for realmente necessário.
→ Use protected quando quiser permitir acesso em subclasses.
→ O modificador padrão é útil para organizar classes dentro do mesmo pacote.

Resumo:

>> Modificadores de Visiblidade: Indica o nível de acesso aos componentes internos de uma classe(atributos e métodos).

piblic[+]: A classe atual e todas as outras classes possam ter acesso a ela(todo mundo vai poder ver ou usar esse atributo).

protected[#]: A classe atual e todas as suas sub-classes.

private[-]: Somente a classe atual.



















